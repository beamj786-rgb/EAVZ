using System.Windows.Forms;

namespace WinFormsApp3
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }

        List<string[]> questions;

        Random rng = new Random();
        private void button1_Click(object sender, EventArgs e)
        {
            if (OpenFileDialog1.ShowDialog() != DialogResult.OK)
                return;
            string path = OpenFileDialog1.FileName;

            FileStream file;
            try
            {


                file = File.Open(path, FileMode.Open, FileAccess.Read, FileShare.Read);
            }
            catch (Exception ex)
            {
                MessageBox.Show(ex.Message);
                return;
            }
            List<string[]> records = new List<string[]>();
            try
            {
                StreamReader reader = new StreamReader(file);
                while (true)
                {
                    string line = reader.ReadLine();
                    if (line == null)
                        break;
                    string[] parts = line.Split(';');

                    if (records.Length < 3)
                    {
                        continue;


                    }
                    records.Add(record);
                }
            }
            catch (Exception ex)
            {
                MessageBox.Show($"Cannot open file {path}: {ex.Message}");
                return;

            }
            if (records.Count < 1)
            {
                MessageBox.Show($"File {path} file could not open");
                return;

            }
        }

        void AskQuestion()
        {


        }

        private void openFileDialog1_FileOk(object sender, System.ComponentModel.CancelEventArgs e)
        {

        }

        private void openFileDialog1_FileOk_1(object sender, System.ComponentModel.CancelEventArgs e)
        {

        }
    }

}
